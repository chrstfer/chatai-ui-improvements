# Created 2026-09-06 Sun 04:48
* Planning
** General
*** Architecture Choices
- Modularization:
  1. *Site Adapters* handle the source DOM environment (Gemini, Claude, ChatGPT).
  2. *Language Modules* handle the content payload (Org-mode, Markdown, LaTeX, Mermaid).
  3. *Tree-Shaking & Dynamic Loading*:
     - Heavy parsers (e.g., full Org grammar, Prism/Shiki tokenizers, KaTeX for
       math blocks) can be loaded asynchronously on-demand via dynamic imports
       when a specific code block language is detected.
  4. *Clear Extensibility Contract*:
     - Every language module implements a standard `LanguageDefinition`
       interface, making new language support entirely pluggable without
       modifying core runtime files.
*** Imperative Invariants
**** Core Architectural Terminology & Definitions
***** 1. Structural & Isolation Roles
- *AST (Abstract Syntax Tree)* :: A pure, in-memory tree representation of parsed markup or code (e.g., Org-mode syntax nodes). It is completely decoupled from any UI framework, DOM node, or platform API.
- *Host Application* :: The third-party web application (e.g., Gemini, ChatGPT, Claude) running in the browser tab that the extension inspects and augments.
- *Host Node* :: A native DOM element created and managed by the Host Application's internal framework (e.g., a native ~<code-block>~ or message container).
- *Injection Container* :: A lightweight wrapper element created by the extension and inserted as a sibling or wrapper to a Host Node to serve as the mounting anchor for custom UI.
- *Shadow Root Boundary* :: An encapsulated DOM subtree (~mountContainer.attachShadow({ mode: 'open' })~) isolating extension styles (~adoptedStyleSheets~) and Preact component trees from host CSS resets and global styles.
***** 2. Architectural Layers & Components
- *Pure Domain Layer (~*/ast/~, ~*/model/~, ~*/contracts/~)* :: Headless, deterministic TypeScript modules containing domain models, parsing logic, AST manipulation, and type definitions. Completely free of DOM and WebExtension APIs.
- *Scraper (~src/chat/<provider>/scraper/~)* :: A DOM-reading module responsible for locating host elements, extracting raw payload strings, detecting message IDs, and inspecting host streaming markers. It does not manage lifecycle or render UI.
- *Controller / Adapter (~src/chat/<provider>/controller/~)* :: The orchestration layer that connects Scrapers, background services, caching layers, and View lifecycles. It listens to DOM mutations, manages the settlement state machine, and triggers mounting/unmounting.
- *View Layer (~*/views/~, ~*/ui/~)* :: Preact component trees, hooks, and localized styles that render parsed domain ASTs into visual interfaces inside the Shadow Root.
- *Platform / Storage Adapter (~src/core/storage/~, ~src/core/rpc/~)* :: The module wrapping browser-specific runtime APIs (~browser.storage~, ~browser.runtime.sendMessage~) behind typed Promise-based contracts.
***** 3. Lifecycle & State Machine Concepts
- *Micro-Debouncing (Observer Batching)* :: A short execution delay (30–50ms or ~requestAnimationFrame~) applied to ~MutationObserver~ events to coalesce high-frequency DOM changes before processing.
- *Macro-Settling (Stream Finalization)* :: The state-machine heuristic (e.g., >300ms mutation silence or presence of host completion indicators) used to determine that the LLM has finished streaming a response.
- *In-Flight Phase (Streaming)* :: The transient state during token arrival where heavy AST parsing and interactive Preact mounting are suspended in favor of lightweight native/text updates.
- *Settled Phase (Finalized)* :: The stable state reached after token generation completes, triggering full AST parsing, syntax highlighting, and interactive Preact view mounting.
- *Mount Guard* :: Explicit HTML dataset attributes (~data-ext-processed~, ~data-ext-mounted~) set on DOM elements to make scrapers and injectors strictly idempotent across multiple observer passes.
- *Decoupled LRU Cache* :: An in-memory cache keyed by stable host message IDs or content hashes that stores parsed ASTs and view state independent of physical host DOM node lifecycle (surviving virtual-scroller unmounting).
**** System Architecture & Extension Ruleset
***** 1. Architectural Layers & Boundary Invariants
The codebase enforces a strict directional dependency graph:

~Domain (Pure)~ <- ~Store/Adapters~ <- ~Controllers/Scrapers (DOM)~ <- ~Views/UI (Preact/Shadow DOM)~
****** 1.1 Pure Domain & Headless Layer (~*/ast/~, ~*/model/~, ~*/contracts/~)
- 100% pure TypeScript/ECMAScript with zero external runtime dependencies.
- STRICTLY FORBIDDEN: Preact, React, JSX/TSX, CSS/style imports, Browser DOM APIs (~window~, ~document~, ~HTMLElement~, ~MutationObserver~), and WebExtension APIs (~browser.*~).
- Invariant: Must execute deterministically across headless targets (MV3 ~background.ts~ worker, Web Workers, Node.js unit tests).
****** 1.2 Platform & Storage Adapters (`src/core/storage/`, `src/core/rpc/`):
- Encapsulates WebExtension APIs exclusively via the standard `browser.*` namespace.
- Direct invocation of `browser.storage.local` across arbitrary UI or scraper modules is forbidden; all persistent operations route through `src/core/storage/storage.ts`.
- RPC message contracts use native Promise resolution over `browser.runtime.sendMessage` and `browser.runtime.onMessage`.
****** 1.3 DOM Scraping & Host Controllers (~src/chat/*/controller/~, ~src/chat/*/scraper/~)
- Manages host DOM inspection, mutation listening, and lifecycle orchestration.
- May consume Browser DOM APIs, platform storage, and pure domain contracts.
- STRICTLY FORBIDDEN: Direct UI framework imports (Preact/React/JSX) and direct HTML rendering.
- Telemetry & Fallbacks: Selectors must prioritize semantic custom elements (e.g., ~model-response~, ~code-block~) with secondary structural fallbacks (e.g., ~pre > code~). Scrapers must run non-throwing diagnostic health checks on load to log DOM schema drift.
****** 1.4 Presentation & View Layer (~*/views/~, ~*/ui/~, ~src/views/~)
- Houses Preact components, hooks, and stylesheets.
- Mounts inside isolated Shadow Roots attached to host injection containers.
- May import domain contracts and controllers; domain layers must NEVER import presentation layers.
****** 1.5 Module Packaging & Deep Import Invariant
- Barrel files (~index.ts~) must NEVER co-export headless domain logic alongside UI components.
- Headless execution targets must import explicit deep paths (e.g., ~@/languages/org/ast~) to guarantee zero DOM/CSS evaluation during tree-shaking.
***** 2. Host DOM Integration & Mutation Lifecycle
****** 2.1 Non-Destructive Host Mutation
- Never call ~.remove()~, clear ~.innerHTML~, or alter the internal children of host SPA nodes.
- Hide superseded host nodes strictly via styling properties (~display: none~ or ~hidden~ attribute) to preserve host virtual DOM state, event listeners, and native actions (copy, retry).
****** 2.2 Idempotent Mount Guards
- Nodes targeted for processing must be tagged before execution:
  - ~element.dataset.extProcessed = "true"~ (scraped / parsed)
  - ~element.dataset.extMounted = "true"~ (UI active)
- All injection and scrape routines must verify dataset tags before acting to guarantee idempotency across duplicate observer dispatches.
- The adapter must inspect stream completion markers (e.g., checking if the message has settled or verifying mutation silence >300ms) before transitioning a code block from the streaming buffer state to the settled Preact mount state.
****** 2.3 MutationObserver Batching vs. Stream Settling
- Mutation Batching: Wrap observer handlers in ~requestAnimationFrame~ or short micro-debouncing (30–50ms) to batch continuous host streaming events. Filter out mutations originating from extension-tagged subtrees.
- Stream Settlement State Machine:
  - Phase A (In-Flight / Streaming): Maintain native view or lightweight plaintext streaming buffer while active stream markers (mutation frequency, typing cursors) exist.
  - Phase B (Finalized / Settled): Trigger complete AST generation and Preact component mounting only after stream termination criteria are met (host completion indicators or >300ms mutation silence).
****** 2.4 View Lifecycle & Cleanup Registry
- Every mounted Preact root must be registered in a centralized ~Map<string, { root: HTMLElement, destroy: () => void }>~.
- Adapters must provide an idempotent ~destroy()~ method that runs ~render(null, shadowRoot)~, removes injected sibling containers, and restores host element visibility.
***** 3. View Encapsulation & Memory Management
****** 3.1 Shadow DOM Boundary
- Mount interactive views inside open Shadow Roots: ~mountContainer.attachShadow({ mode: 'open' })~.
- Inject component stylesheets via ~shadowRoot.adoptedStyleSheets = [sheet]~ using ~new CSSStyleSheet()~.
- For non-shadow injection wrappers, prefix all selectors with ~.ext-*~ and explicitly reset host typography inheritance.
****** 3.2 Decoupled In-Memory Cache (Virtual Scroller Defense)
- Do not store state on host DOM nodes subject to virtual unmounting/recycling.
- Cache parsed ASTs and view state in a decoupled in-memory LRU cache keyed by a content hash. Do not rely on the host created UUIDs.
- scrolling a recycled message into the viewport must read from the LRU cache and hydrate immediately without re-parsing raw text.
** Source Tree Hierarchy
All code in the following section is intended as a basic example. Contradictions
with the previous rules must be rectified in favor of the rules, not the example
code.
*** src/
**** File: manifest.json
**** File: index.ts
**** core/
***** contracts/
****** File: adapter.ts
****** File: language.ts
****** File: documentView.ts
***** utils/
**** store/
**** languages/
***** Files: registry.ts
***** JSON/
***** LaTeX
Powered by KaTeX.js
***** org/
****** ast/
****** views/
****** styles/
**** chat/
***** registry.ts
***** `gemini/`
****** `model/`
******* File: `conversation.ts`
******* File: `message.ts`
******* File: `codeBlock.ts`
- *Edge Case:* Gemini often renders copy/action buttons in the header container that contain icon text or accessible labels (e.g., "Copy", "Expand") in text nodes /before/ or /after/ the language label depending on shadow/flex order.
- *Failure Mode:* ~walker.nextNode()~ can capture "copy" or whitespace labels as the language tag.
- *Remediation:* Target the specific header container class/tag (e.g., `.code-block-decoration`, `.header-content`) rather than walking raw text nodes of the entire root `codeBlockEl`.
****** `controller/`
******* File: `scraper.ts`
******* File: `injector.ts`
******* File: `domObserver.ts`
`domObserver.ts` (Controller): Owns the raw DOM mutation lifecycle, debouncing, and event emission. It should have no knowledge of specific views or Preact injection.
****** `views/`
Gemini specific views, preact wrappers for general views and some buttons
******* File: `MessageActions.tsx`
******* File: `OrgViewWrapper.tsx`
****** File: `index.ts`
`index.ts` (Entrypoint): Glues the observer, scraper, and injector together, acting as the public-facing facade for the extension's site loader.
****** File: `selectors.ts`
****** File: `types.ts`
**** views
***** common/
***** settings/
**** styles/
***** File: globals.css
**** entrypoints/
** Adapter Specifics
*** Gemini Adapter
**** Notes and Tips
***** Implementation Strategy & Edge Case Mitigations
****** 1. Plaintext Extraction vs Highlighted Syntax
- For language parsing, inspect the header node's `childNodes` for the leading
  text node before the button container. Move on to language detection if it
  reports ~Code snippet~ or ~Plain text~ to identify languages we support that
  the chat does not.
****** 2. Streaming DOM & Debounced Injection
- Model responses stream incrementally. Do not attempt full AST parses on every tiny text mutation.
- Use a `MutationObserver` targeting `infinite-scroller` / `.conversation-container` with `childList: true, subtree: true`.
- Check `dataset` flags (e.g. `data-ext-processed="true"`) to prevent re-parsing completed blocks and avoid layout thrashing.
**** Gemini Scraper Details
The following are entirely early planning notes, and no architectural suggestion
or open question should be taken as contradicting the hard rules elsewhere in
this document. 
***** Structure Overview
The headings reflect CSS selectors relative to their parent containers.
****** chat-window > chat-window-content > infinite-scroller > .conversation-container
******* user-query > .user-query-container
******** user-query-content.user-query-container .query-text p.query-text-line
- Contains user query text.
- Exactly $n + 1$ ~p.query-text-line~ elements exist for $n$ newlines in the query.
******* model-response > response-container
- Contains the ~jslog~ attribute.
******** .response-container-header
******** .presented-response-container
********* .response-container-content
********** .response-content
*********** structured-content-container.model-response-content
************ .container > message-content
- Top-level message content container.
************* .markdown.markdown-main-panel.md-content
- Plaintext and formatted output rendered as standard HTML elements (~<b>~, ~<h1>~–~<h6>~, ~<p>~, etc.).
************* .container > message-content > response-element
************** code-block
- Direct child of ~response-element~.
- Language identification logic:
  - Language label is stored in the often child ~TEXT_NODE~ preceding the ~<code>~ element.
  - Known syntax-highlighted languages contain nested HTML formatting inside the code node.
  - "Code snippet": pre-formatted plain text without internal syntax highlighting elements.
  - "Plain text": standard plain text format.
  - Absence of a leading ~TEXT_NODE~ before the ~<code>~ element indicates a
    missing native header bar/buttons.
    - This is not necessarily reliable.
- if ~headerTextNode~ is absent, evaluate regex patterns against the first 3 lines of ~rawText~ (e.g., ~^\*+\s~ for Org headlines or ~^#\+TITLE:~ for Org headers).
*************** code
- Descendant of ~code-block~.
- Contains raw text or nested HTML syntax-highlighting nodes depending on language detection.
******** .response-container-footer message-actions[footer]
- Container for message-level actions.
- Child structure:
  - ~<thumb-up-button>~
  - ~<thumb-down-button>~
  - ~<regenerate-button>~
  - ~<copy-button>~
  - ~<gem-icon-button>~ (Overflow menu ~...~)
