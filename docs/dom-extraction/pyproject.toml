[project]
name = "dom-extractor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chrstfer", email = "chrstfer@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "lxml>=6.1.3",
]

[project.scripts]
extractor = "dom_extractor:main"


[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
